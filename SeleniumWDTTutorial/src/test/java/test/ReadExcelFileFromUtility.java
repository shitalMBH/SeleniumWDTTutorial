package test;

public class ReadExcelFileFromUtility {

	public static void main(String[] args) {
		// How to use excel file Utility and use in test script


				/*
				 * WebElement name =
				 * driver.findElement(By.id("name")).sendKeys(ExcelReader.readData(0,0));
				 * WebElement lastName =
				 * driver.findElement(By.id("last_name")).sendKeys(ExcelReader.readData(0,0));
				 * WebElement email =
				 * driver.findElement(By.id("email")).sendKeys(ExcelReader.readData(0,0));
				 * WebElement password =
				 * driver.findElement(By.id("password")).sendKeys(ExcelReader.readData(0,0));
				 * WebElement confirmPassword =
				 * driver.findElement(By.id("password_confirmation")).sendKeys(ExcelReader.
				 * readData(0,0));
				 */

	}

}
